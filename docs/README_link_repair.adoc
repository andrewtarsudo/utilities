= Руководство для link-repair
:experimental:
:icons: font
:source-highlighter: rouge
:imagesdir: images
:figure-caption: Рисунок
:stylesdir: ../css
:stylesheet: default.css
:nofooter:
:toc: auto
:toc-title: Содержание
:sectnums:
:name: tw_utilities.exe
:script-name: link-repair

== Описание

Скрипт проверяет и исправляет некорректные ссылки в файлах проекта.

== Для чего нужно?

Зачастую файлы перемещаются внутри проекта, удаляются, переименовываются, а ссылки на них внутри других файлов не исправляются.
Также при задании якорей необходимо, чтобы они были уникальными в рамках всего проекта, не только отдельного файла.

include::partials/run.adoc[]

=== Опции скрипта

[source,console,subs="attributes"]
----
Использование:
py {name} {script-name}
-d/--dry-run
-n/--no-result
-a/--anchor-disable
-s/--separate
--skip-en
--keep-logs
PATHDIR

  Команда для проверки и исправления ссылок в файлах документации

Аргументы:
  pathdir                                Директория, в которой обрабатываются
                                         все *.md- и *.adoc-файлы.

Опции:
  -d, --dry-run                          Флаг вывода некорректных ссылок на экран без изменения файлов.
                                         По умолчанию: False, файлы перезаписываются
  -n, --no-result                        Флаг удаления файла с результатами работы скрипта по завершении
                                         работы в штатном режиме.
                                         По умолчанию: False, файл сохраняется
  -a, --anchor-disable                   Флаг поиска повторяющихся якорей.
                                         По умолчанию: True, поиск дублирующихся якорей осуществляется
  -s, --separate                         Флаг раздельной обработки файлов на различных языках.
                                         По умолчанию: True, файлы на русском и английском обрабатываются
                                         отдельно
  --skip-en                              Флаг обработки файлов только на русском языке.
                                         По умолчанию: False, обрабатываются файлы на обоих языках.
  --keep-logs                            Флаг сохранения директории с лог-файлом по завершении работы
                                         в штатном режиме.
                                         По умолчанию: False, лог-файл и директория удаляются
  -h, --help                             Вывести справочную информацию на
                                         экран и завершить работу
----------------------------------------------------------------------------------------------------
----

NOTE: Путь может быть как абсолютным, так и относительным.