[[terms]]
= Руководство для terms
:imagesdir: images
:stylesdir: ../css
:stylesheet: default.css
:pdf-themesdir: themes
:pdf-theme: base-theme.yml
:script-name: terms
:asciidoctorconfigdir: ..

== Описание

Скрипт выводит расшифровки аббревиатур и сокращений, используемых в документации продуктов сетевых и мобильных технологий.

== Для чего нужно?

При создании или изменении документа часто встречаются широко используемые аббревиатуры, которые не всегда с первого взгляда ясны и понятны.
Также необходимо добавлять пояснения к сокращениям по тексту, поскольку достаточно многие имеют несколько возможных вариантов интерпретации.

include::partials/run.adoc[]

=== Опции скрипта

[source,console,subs="attributes+"]
----
Использование:
{name} {script-name} <TERMS>
--a/--all | -f/--full | -i/--info | -r/--readme | -s/--samples | --abbr | --ascii | --common |
-k/--keep-logs | --h/--help

  Команда для вывода расшифровки аббревиатур

Аргументы:
  terms (*)                              Перечень запрашиваемых аббревиатур

Опции:
  -a, --all                              Флаг вывода всех сокращений.
                                         Примечание. Не может использоваться одновременно с
                                         --full, --readme, --samples
  -f, --full                             Флаг вывода всех сокращений с их расшифровками.
                                         Примечание. Не может использоваться одновременно с
                                         --all, --readme, --samples
  -i, --info                             Флаг вывода полного руководства.
                                         Примечание. Не может использоваться одновременно с
                                         --full, --all, --samples, --readme
  -r, --readme                           Флаг вывода полного руководства.
                                         Примечание. Не может использоваться одновременно с
                                         --full, --all, --samples, --info
  -s, --samples                          Флаг вывода примеров использования.
                                         Примечание. Не может использоваться одновременно с
                                         --full, --readme, --all, --info
  --abbr                                 Флаг вывода сокращения для добавления в файл Markdown.
                                         Формат: <abbr title=""></abbr>.
                                         Примечание. Не может использоваться одновременно с
                                         --ascii, --common
  --ascii                                Флаг вывода сокращения для добавления в файл AsciiDoc.
                                         Формат: pass:q[<abbr title=""></abbr>].
                                         Примечание. Не может использоваться одновременно с
                                         --abbr, --common
  --common                               Флаг вывода сокращения в свободном виде.
                                         Примечание. Не может использоваться одновременно с
                                         --abbr, --ascii
  {keep-logs}
                                         {keep-logs-cont}
                                         {keep-logs-default}
  {help}
                                         {help-cont}
-------------------------------------------------------------------------------------------------
----

=== Примеры

include::../sources/samples.txt[]